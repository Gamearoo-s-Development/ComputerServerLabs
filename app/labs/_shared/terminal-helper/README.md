# Deprecated: use `labs/_shared/lab-workstation`

The Lab Terminal now uses the **Ubuntu lab-workstation** image (`sysadmin-game/lab-workstation:latest`).

This folder is kept only for older references; do not build `terminal-helper` for new sessions.

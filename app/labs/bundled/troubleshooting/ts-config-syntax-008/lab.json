{
  "id": "ts-config-syntax-008",
  "title": "Fix Config Syntax",
  "difficulty": "Medium",
  "category": "Troubleshooting",
  "bundled": true,
  "estimatedTimeMinutes": 30,
  "description": "Practice nginx -t in an isolated Docker training environment. Complete the briefing tasks and create the completion marker.",
  "labMode": "target-only",
  "runtime": "docker",
  "docker": {
    "image": "sysadmin-game/ts-config-syntax-008:latest",
    "buildPath": "..",
    "ports": []
  },
  "tags": [
    "troubleshooting",
    "nginx",
    "bundled"
  ],
  "safetyNotes": [
    "Commands run only inside the lab container — not on your host.",
    "Do not paste destructive commands (rm -rf /, mkfs, dd to disks) even inside the lab."
  ],
  "tasks": [
    "Open the lab terminal (session user is already logged in)",
    "Read the briefing in ~/README.txt about nginx -t",
    "Create the read marker: touch /tmp/ts-config-syntax-008-read",
    "Create the completion marker: touch /tmp/ts-config-syntax-008-complete",
    "Use Check in the lab panel — the lab completes when objectives pass"
  ],
  "objectivesPublic": [
    {
      "id": "read-briefing",
      "label": "Read the briefing",
      "text": "Read ~/README.txt and mark it read.",
      "hint": "Run: touch /tmp/ts-config-syntax-008-read"
    },
    {
      "id": "complete-lab",
      "label": "Finish the exercise",
      "text": "Apply what you learned about nginx -t and create the completion marker.",
      "hint": "Run: touch /tmp/ts-config-syntax-008-complete"
    }
  ],
  "objectives": [
    {
      "id": "read-briefing",
      "autoCheck": "fileExists",
      "path": "/tmp/ts-config-syntax-008-read"
    },
    {
      "id": "complete-lab",
      "autoCheck": "fileExists",
      "path": "/tmp/ts-config-syntax-008-complete"
    }
  ],
  "hints": [
    "Focus on nginx -t.",
    "Use ls, cat, and touch — no root privileges required.",
    "Completion marker path: /tmp/ts-config-syntax-008-complete"
  ],
  "successCriteria": [
    "Briefing read marker exists at /tmp/ts-config-syntax-008-read",
    "Completion marker exists at /tmp/ts-config-syntax-008-complete"
  ],
  "validation": {
    "type": "fileExists",
    "path": "/tmp/ts-config-syntax-008-complete"
  },
  "postLabReview": {
    "summary": "You practiced nginx -t in a safe container.",
    "skillsPracticed": [
      "Fix Config Syntax reinforces everyday sysadmin skills.",
      "Always verify files with ls -l after creating markers.",
      "Repeat the lab to build muscle memory."
    ]
  },
  "xpReward": 80,
  "unlockRequirements": {
    "minLevel": 6,
    "requiredLabs": [
      "ts-permission-denied-007"
    ]
  }
}

# docker-cli-001 — Docker CLI Check

Target-only lab with `docker.io` installed. Learners run `docker version` inside the lab container.

**Note:** This uses the Docker CLI against the engine available in the training image; it is not a nested DinD lab.
